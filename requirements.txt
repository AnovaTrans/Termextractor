# Streamlit + starlette are pinned to a verified-compatible pair. With only `>=`
# bounds, every Streamlit Cloud rebuild floated to the latest of both, and a
# newer starlette made GZipResponder's `thread_minimum_size` a required argument
# that Streamlit's gzip middleware does not pass — crashing every HTTP request
# with "GZipResponder.__init__() missing 1 required keyword-only argument".
streamlit==1.51.0
starlette==1.3.0

anthropic>=0.40.0
pyyaml>=6.0
python-docx>=0.8.11
PyPDF2>=3.0.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
pandas>=2.0.0,<3.0     # pandas 3.0 is a breaking major; stay on the tested 2.x line
lxml>=4.9.0
